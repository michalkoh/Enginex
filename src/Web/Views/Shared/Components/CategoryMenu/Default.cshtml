@model IEnumerable<string>

<div class="container">
    <div class="row justify-content-center mb-5">
        <div class="col-md-12 nav-link-wrap">
            <div class="nav ftco-animate nav-pills" id="v-pills-tab" role="tablist" aria-orientation="vertical">
                @foreach (var category in Model)
                {
                    <a class="nav-link active" id="v-pills-1-tab" data-toggle="pill" href="#v-pills-1" role="tab" aria-controls="v-pills-1" aria-selected="true">@category</a>
                }
            </div>
        </div>
    </div>
</div>

        @*@foreach (var category in Model)
        {
            <a class="btn btn-block @(ViewBag.SelectedCategory == category ? "btn-primary" : "btn-secondary")"
               asp-controller="Product"
               asp-action="List"
               asp-route-category="@category"
               asp-route-page="1">
                @category
            </a>
        }*@
